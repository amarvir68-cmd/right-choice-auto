 'use client';
import {useState} from 'react';
export default function RepairRequest(){
 const [busy,setBusy]=useState(false),[result,setResult]=useState(''),[success,setSuccess]=useState(false);
 async function submit(e){e.preventDefault();setBusy(true);setResult('');setSuccess(false);const form=e.currentTarget;const data=Object.fromEntries(new FormData(form));try{const res=await fetch('/api/inquiries',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...data,kind:'repair',consent:data.consent==='on'})});const body=await res.json();if(!res.ok)throw Error(body.error||'Unable to send request.');form.reset();setSuccess(true);setResult('Request received. Our team will contact you to confirm; this is not a booked appointment.');}catch(err){setSuccess(false);setResult(err.message)}finally{setBusy(false)}}
 return <section className="section repairRequest" id="request-service"><div className="repairIntro"><p className="eyebrow">REQUEST SERVICE</p><h2>Request a repair appointment</h2><p>Tell us what you need. We will contact you to confirm a date and time. Submitting this form does not reserve an appointment.</p></div><form className="repairRequestForm" onSubmit={submit} aria-label="Repair appointment request"><label>Name<input name="name" required maxLength="100" autoComplete="name"/></label><label>Email<input name="email" type="email" required maxLength="180" autoComplete="email"/></label><label>Phone<input name="phone" type="tel" maxLength="35" autoComplete="tel"/></label><label>Service needed<select name="service" required defaultValue=""><option value="" disabled>Select a service</option>{['Oil change','Brakes','Diagnostics','Tires and wheels','Suspension and steering','General repair','Other'].map(x=><option key={x}>{x}</option>)}</select></label><label>Vehicle (optional)<input name="vehicle" maxLength="120" placeholder="Year, make and model"/></label><label>Preferred date (optional)<input name="preferredDate" type="date" min={new Date().toISOString().slice(0,10)}/></label><label className="repairRequestWide">What can we help with?<textarea name="message" required minLength="10" maxLength="2000" rows="4"/></label><div className="formHoneypot" aria-hidden="true"><label>Website<input name="website" tabIndex="-1" autoComplete="off"/></label></div><label className="repairRequestWide inquiryConsent"><input name="consent" type="checkbox" required/> I agree to be contacted about my repair request.</label><button className="btn red" disabled={busy}>{busy?'Sending…':'Send appointment request'}</button><p className={`repairRequestWide repairFeedback ${success?'isSuccess':'isError'}`} role={success?'status':'alert'} aria-live="polite">{result}</p></form></section>;
}
